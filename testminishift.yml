---
- name: minishift project create
  hosts: all
  gather_facts: false
  connection: local

  collections:
    - community.kubernetes
  tasks:
  - name: Create project
    k8s:
      state: present
      inline:
        kind: ProjectRequest
        metadata:
          name: vodatestproject
        displayName: vodademo
        description: vodafone
